plugins {
    // Permet à Gradle de télécharger automatiquement le JDK requis (21) s'il n'est
    // pas déjà installé sur la machine, au lieu d'échouer avec "Cannot find a Java
    // installation... Toolchain download repositories have not been configured."
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

include ':app'
include ':capacitor-cordova-android-plugins'
project(':capacitor-cordova-android-plugins').projectDir = new File('./capacitor-cordova-android-plugins/')

apply from: 'capacitor.settings.gradle'