# Paramètres pnpm (pnpm ≥ 10.34 ne lit plus le champ "pnpm" de package.json).
# sharp (dépendance de @capacitor/assets) doit compiler son binaire à l'installation.
onlyBuiltDependencies:
  - sharp

# Le wasm doit venir du sql.js figé dans le bundle de jeep-sqlite 2.8.0 (#152)
overrides:
  jeep-sqlite>sql.js: 1.11.0
